{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/nf-core/cfdna/master/assets/schema_input.json",
    "title": "nf-core/cfdna pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "fastq": {
                "type": "string",
                "format": "file-path",
                "exists": true,
                "pattern": "^\\S+\\.f(ast)?q(\\.gz)?$",
                "errorMessage": "FastQ file for reads 1 must be provided, cannot contain spaces and must have extension '.fq.gz','.fastq.gz' or fq"
            },
            "purity": {
                "type": "number",
                "pattern": "^\\d+$",
                "errorMessage": "Aprroximate tumor purity must be provided for each sample in the form of a fraction, from 0 to 1 (1 being 100% pure)."
            }
        },
        "required": ["sample", "fastq", "purity"]
    }
}
